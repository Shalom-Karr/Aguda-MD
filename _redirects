# Cloudflare Pages and Netlify automatically serve `/foo.html` at `/foo`.
# No rewrite rules are needed for clean URLs — adding them causes redirect loops.
#
# This file is kept as a place to add redirects later (e.g. for renamed pages).
