# Cloudflare Pages and Netlify automatically serve `/foo.html` at `/foo`.
# No rewrite rules are needed for clean URLs to .html files.

# Clean URLs for program guides: /posts/snap serves posts.html?title=snap
# while keeping /posts/snap in the URL bar (200 = rewrite, not redirect).
/posts/:slug   /posts.html?title=:slug   200
